# Dependencias — en cada servidor: npm ci (no subir node_modules)
**/node_modules/

# Angular — en cada servidor: ng build (adminSPA/.gitignore ya ignora /dist; refuerzo desde raíz)
adminSPA/dist/

# Logs y depuración local
*.log
debug-*.log

# Scripts de parche locales (no subir)
backAppC/scripts/_*.js
backAppC/utils/bash_write_test.txt

# Carpetas locales: documentación, respaldos, copias de consulta, artefactos SUNAT de referencia
.cursor/
Consulta/
deploy/
docs/
documentacion/
ops/
otros/
respaldo/
CRM/
infra/
analisis-grupo-salvacion/
# Flayers PNG/marketing locales (no subir). HTML del sitio va en adminSPA/public/flayers/
/flayers/

# Caché / pruebas (por si se generan fuera de adminSPA/.gitignore)
**/.angular/cache/
**/coverage/
**/playwright-report/
**/test-results/

# Tests / e2e / pentest — no versionar
scripts/verify.mjs
adminSPA/jest.config.js
adminSPA/setup-jest.ts
adminSPA/tsconfig.spec.json
adminSPA/playwright.config.js
adminSPA/e2e/
adminSPA/**/*.spec.ts
backAppC/scripts/pentest-*.js
backAppC/scripts/test-*.js
backAppC/scripts/e2e*.js
backAppC/test-*.js
backAppC/test_*.js

# Variables de entorno — usar solo *.example versionados
.env
.env.local
.env.production
.env.development
**/.env.local
backAppC/.env
pdf-backend/.env
adminSPA/.env
whatsapp-gateway/.env
# Sesiones Baileys (credenciales WhatsApp por empresa — nunca versionar)
whatsapp-gateway/sessions/
whatsapp-gateway/sessions/**
# Notas / scripts de prueba con datos sensibles (no subir)
planes_comerciales_saas_fb6b3258.plan.md
test_login.js

# Copias de base de datos accidentales en el repo
*.bak

# QR / fotos de pago del bot WhatsApp (por empresa)
backAppC/uploads/formas-pago/

# PNG/JPG de flayers comerciales (el bot los envía si existen)
backAppC/uploads/flayers-comercial/*
!backAppC/uploads/flayers-comercial/.gitkeep

# Sistema
.DS_Store
Thumbs.db

# Plantillas SIN secretos: sí se versionan (p. ej. backAppC/.env.example, pdf-backend/.env.example)
