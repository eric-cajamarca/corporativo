# Dependencias — en cada servidor: npm ci (no subir node_modules)
**/node_modules/

# Angular — en cada servidor: ng build (adminSPA/.gitignore ya ignora /dist; refuerzo desde raíz)
adminSPA/dist/

# Logs y depuración local
*.log
debug-*.log

# Scripts de parche locales (no subir)
backAppC/scripts/_*.js
backAppC/utils/bash_write_test.txt

# Carpetas locales: documentación, respaldos, copias de consulta, artefactos SUNAT de referencia
.cursor/
Consulta/
deploy/
docs/
documentacion/
ops/
otros/
respaldo/
CRM/


# Caché / pruebas (por si se generan fuera de adminSPA/.gitignore)
**/.angular/cache/
**/coverage/
**/playwright-report/
**/test-results/

# Variables de entorno — usar solo *.example versionados
.env
.env.local
.env.production
.env.development
**/.env.local
backAppC/.env
pdf-backend/.env
adminSPA/.env

# Notas / scripts de prueba con datos sensibles (no subir)
planes_comerciales_saas_fb6b3258.plan.md
test_login.js

# Copias de base de datos accidentales en el repo
*.bak

# Sistema
.DS_Store
Thumbs.db

# Plantillas SIN secretos: sí se versionan (p. ej. backAppC/.env.example, pdf-backend/.env.example)
